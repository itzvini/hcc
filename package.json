{
  "name": "hcc-player-council",
  "version": "1.0.0",
  "private": true,
  "description": "Static HCC Player Council announcement site for Railway deployment.",
  "main": "server.js",
  "scripts": {
    "start": "node --env-file-if-exists=.env server.js",
    "dev": "node --env-file-if-exists=.env server.js"
  },
  "dependencies": {
    "pg": "^8.13.1"
  },
  "engines": {
    "node": ">=20"
  }
}

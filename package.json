{
  "name": "hcc-site",
  "version": "1.0.0",
  "private": true,
  "description": "The Highrise Creature Club member site — club hub, market data, in-wallet trading, holders, roadmap, and the Player Council election. Static front-end served by a small Node server on Railway.",
  "main": "server.js",
  "scripts": {
    "start": "node --env-file-if-exists=.env server.js",
    "dev": "node --env-file-if-exists=.env server.js"
  },
  "dependencies": {
    "@imtbl/orderbook": "^2.20.0",
    "@noble/curves": "^1.2.0",
    "@noble/hashes": "^1.3.2",
    "pg": "^8.13.1"
  },
  "engines": {
    "node": ">=20"
  }
}
